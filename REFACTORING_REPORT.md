# تقرير فحص وإصلاح مستودع Whatsapp_web

تم فحص المستودع وتنفيذ الإصلاحات المطلوبة المتعلقة بإشعارات FCM، حالات التسليم والقراءة، سياسات Supabase، دورة حياة رموز الإشعارات، وعارض الوسائط.

## التغييرات المنفذة

| المجال | التغيير | الحالة |
|---|---|---|
| إشعارات FCM | إزالة إعدادات `notification` من رسالة FCM وإرسال بيانات فقط، مع تمرير العنوان والنص والأيقونة ورابط الفتح داخل `data` | مكتمل |
| Service Worker | إضافة ذاكرة قصيرة المدى لمنع تكرار الإشعار عند إعادة تسليم الرسالة نفسها، واستخدام `messageId` ضمن `tag` | مكتمل |
| الواجهة الأمامية | منع تشغيل الصوت أو تحديث واجهة الإشعار عندما تكون الصفحة في الخلفية؛ يقتصر مستمع foreground على الصفحة النشطة | مكتمل |
| تسجيل الخروج | حذف صف رمز FCM من Supabase، إبطال الرمز عبر Firebase `deleteToken`، وتنظيف التخزين المحلي | مكتمل |
| التسليم والقراءة | عند استلام رسالة واردة تُحدّث إلى `delivered`، وعند فتح المحادثة تُحدّث رسائل الطرف الآخر إلى `read`، وتعرض الواجهة علامة واحدة أو علامتين رماديتين أو علامتين زرقاوين حسب الحالة | مكتمل |
| الحذف والصلاحيات | الحفاظ على حذف الرسائل عبر الدالة المحمية `delete_message_as_moderator` وحذف المستخدم عبر Edge Function المخصصة للمشرف الإداري | مكتمل في نموذج المستودع الحالي |
| SQL/RLS | استبدال صيغة `CREATE POLICY IF NOT EXISTS` غير المدعومة بصيغة قابلة لإعادة التنفيذ تعتمد على `DROP POLICY IF EXISTS` ثم `CREATE POLICY` | مكتمل |
| عارض الوسائط | تغيير زر الإغلاق إلى سهم رجوع واضح أعلى اليسار مع الإبقاء على الإغلاق بالنقر على الطبقة والتحجيم `object-contain` الموجودين | مكتمل |

## الملفات المعدلة

تم تعديل الملفات التالية:

- `firebase-messaging-sw.js`
- `js/app.js`
- `js/push.js`
- `partials/chat-panel.html`
- `sql/fcm_and_rls.sql`
- `supabase/functions/send-push/index.ts`

## نتائج الفحوصات

| الفحص | النتيجة |
|---|---|
| `node --check` لجميع ملفات JavaScript | ناجح |
| تجميع Edge Function الخاصة بالإرسال باستخدام esbuild | ناجح |
| البحث عن صيغة `create policy if not exists` | لا توجد نتائج |
| `git diff --check` | ناجح |
| مراجعة فصل رسائل FCM data-only عن إشعارات النظام | ناجح |
| مراجعة وجود منع التكرار وإبطال التوكن وحالات `delivered` و`read` | ناجح |

## ملاحظة مهمة عن نموذج الصلاحيات

المستودع يستخدم `public.profiles` وعضويات `public.chat_members` بدل جدول `public.users` ذي عمود `role` كما ورد في الطلب الأصلي. لذلك تم الحفاظ على نموذج الصلاحيات الموجود، وهو أكثر اتساقًا مع بقية التطبيق: المشرف الإداري يعتمد على `profiles.is_admin`، ومشرف المحادثة يعتمد على دور `admin` أو `moderator` داخل `chat_members`. لم تتم إضافة جدول بديل قد يتعارض مع المخطط الحالي.

## حالة النشر على GitHub

تم إنشاء commit محلي على الفرع `main` بالمعرّف:

```text
e8c2d91 fix notifications delivery status moderation and media viewer
```

تم التحقق من أن الحساب يملك صلاحية `push` عبر واجهة GitHub، لكن خادم Git رفض عملية الدفع عبر عنوان HTTPS برسالة `403 Permission denied`. لذلك بقي التعديل محفوظًا محليًا في `/home/ubuntu/Whatsapp_web` ولم يتغير المستودع البعيد؛ يمكن دفعه من بيئة GitHub موثوقة باستخدام:

```bash
git push origin main
```

بعد تطبيق SQL في مشروع Supabase، يُنصح باختبار حالتين عمليتين: وصول رسالة إلى صفحة في الخلفية للتحقق من إشعار نظام واحد فقط، وفتح المحادثة من جهاز المستلم للتحقق من الانتقال من `delivered` إلى `read`.

## الملفات المرجعية

هذا التقرير محفوظ داخل المستودع باسم `REFACTORING_REPORT.md`.

> لم يتم تنفيذ اتصال مباشر بقاعدة Supabase أو حذف بيانات فعلية؛ التحقق من SQL هنا هو تحقق ساكن من الصياغة واتساقها مع مخطط المشروع، ويجب تشغيله داخل مشروع Supabase المستهدف قبل الإنتاج.

---

إعداد: Manus AI
تاريخ الفحص: 2026-09-05
